--Configuration for the 12 gauge slug
local config = {}

config.Diameter = 20 --millimeters
config.Mass = 28.35 --grams
config.Name = script.Name
config.Weight = 0.07 -- of the cartridge, kilograms

return config