--Configuration for the 20 gauge slug
local config = {}

config.Diameter = 15.6 --millimeters
config.Mass = 18 --grams
config.Name = script.Name
config.Weight = 0.03 -- of the cartridge, kilograms

return config