--Configuration for the .45 ACP bullet
local config = {}

config.Diameter = 11.5 --millimeters
config.Mass = 15 --grams
config.Name = script.Name
config.Weight = 0.021 -- of the cartridge, kilograms

return config