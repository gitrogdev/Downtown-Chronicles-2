--Configuration for the .32 ACP bullet
local config = {}

config.Diameter = 7.94 --millimeters
config.Mass = 4 --of the projectile, grams
config.Name = script.Name
config.Weight = 0.008 -- of the cartridge, kilograms

return config