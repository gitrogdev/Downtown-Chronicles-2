--Configuration for the .357 Magnum bullet
local config = {}

config.Diameter = 9.1 --millimeters
config.Mass = 13 --grams
config.Name = script.Name
config.Weight = 0.01 -- of the cartridge, kilograms

return config