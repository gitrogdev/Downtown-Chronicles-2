--Configuration for the 6mm 0.25g airsoft BB
local config = {}

config.Diameter = 6 --millimeters
config.Mass = 0.25 --grams
config.Name = script.Name
config.Weight = 0.003 -- of the cartridge, kilograms

return config