--Configuration for the 14.5x114mm bullet
local config = {}

config.Diameter = 14.88 --millimeters
config.Mass = 64 --grams
config.Name = script.Name
config.Weight = 0.2 -- of the cartridge, kilograms

return config